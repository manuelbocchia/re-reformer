
<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
      <% @user.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @user do |form|%>
  <div>
  <%= form.label :username%>
  <%= form.text_field :username, placeholder: "Enter your username" %>
  </div>
<div>
  <%= form.label :email%>
  <%= form.email_field :email, placeholder: "your@email.com" %>
</div>
<div>
  <%= form.label :password %>
  <%= form.password_field :password , placeholder: "secure password" %>
</div>
<div>
  <%= form.submit%>
</div>
<%end%>